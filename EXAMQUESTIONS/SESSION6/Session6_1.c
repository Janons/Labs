#include <stdio.h>
#include <stdlib.h>

void sequence_invert(int *v1, int n, int *v2);

int main()
{
    return 0;
}

void sequence_invert(int *v1, int n, int *v2)
{
    int i, j, k;

    for (i = 0, j = 1; j < n; i++, j++)
    {
        if (v1[j] - v1[i] = 1)
        {
            for (k = 0; k < n - j || ; k++)
            {
                

            }
        }
    }
}