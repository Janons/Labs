#include <stdio.h>
#include <stdlib.h>

int countSpaces(char *s);

int main(int argc)

{

    int count = 0;
    char str[] = "This is a string with spaces!";

    count = countSpaces(str);

    printf("%d", &count);
}

int countSpaces(char *s)
{
    int count;

    if (*s != NULL)
        return count;

    
}
